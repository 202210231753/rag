# 【配置】数据库密码、API Key 等机密信息
# --- MySQL 配置 ---
# 本地代码连接填 localhost，Docker 内部连接填 mysql_db
DB_SERVER=localhost
DB_PORT=3306
DB_USER=rag_user
DB_PASSWORD=rag_password
DB_NAME=rag_data
DB_ROOT_PASSWORD=root_secret

# --- Milvus 配置 ---
# 本地代码连接填 localhost，Docker 内部连接填 milvus-standalone
MILVUS_HOST=localhost
MILVUS_PORT=19530

# --- OpenAI 配置 (LlamaIndex 需要) ---
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- Redis 配置 ---
# 本地代码连接填 localhost，Docker 内部连接填 redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis_secure_password

# --- Elasticsearch 配置 ---
# 本地代码连接填 localhost，Docker 内部连接填 elasticsearch
ES_HOST=localhost
ES_PORT=9200

# --- 同义词功能配置 ---
# 是否在应用启动时自动初始化同义词数据（true/false，默认：true）
SYNONYM_AUTO_INIT=true