# 【依赖】Python 库列表
# --- Web Framework (FastAPI) ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart    # 文件上传必需
pydantic>=2.5.0
pydantic-settings

# --- Database Drivers ---
# MySQL
pymysql>=1.1.0
cryptography
sqlalchemy>=2.0.25

# --- Redis ---
redis[hiredis]>=5.0.0  # 异步Redis客户端

# --- MinIO 对象存储 ---
minio>=7.2.0

# --- LlamaIndex Core & Components (v0.10+) ---
# 核心库
llama-index-core>=0.10.0

# LLM 和 Embedding 模型 (这里以 OpenAI 为例)
llama-index-llms-openai
llama-index-embeddings-openai

# 向量数据库 (Milvus 专用集成包)
llama-index-vector-stores-milvus

# 文件读取器 (用于解析 PDF, Docx 等)
llama-index-readers-file

# --- ElasticSearch (BM25 关键词检索) ---
elasticsearch>=8.11.0
elasticsearch[async]

# --- 中文分词 ---
jieba>=0.42.1

# --- 日志库 ---
loguru>=0.7.0

# --- Rerank 相关依赖 ---
# HTTP 客户端（用于调用 TEI 接口）
httpx>=0.25.0

# 本地重排模型支持（可选，如果使用 LocalRerankModel）
# sentence-transformers>=2.2.0

# --- Utilities ---
python-dotenv>=1.0.0
requests

# --- Chinese Tokenizers ---
# 用于“中文分词模块”：可通过接口在 jieba / HanLP 间切换
jieba>=0.42.1
hanlp>=2.1.0


# --- Model Support (Qwen/HuggingFace) ---

transformers>=4.37.0
accelerate>=0.27.0
sentence-transformers>=2.3.0
